// Circle class defined in external file named Circle.as
dynamic class box
{
	var i : Number;
	
	function box(param)
	{
		i = param;
		trace("box constructor: " + i + ", j=" + j);
	}

	function draw()
	{
		trace("BOX: draw is called: i=" + i + ", j=" + j);
	}
	
}
